import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('yammer-video', 'Integration | Component | yammer video', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
});
