import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('yammer-toggle', 'Integration | Component | yammer toggle', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
});
