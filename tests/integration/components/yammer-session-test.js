import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('yammer-session', 'Integration | Component | yammer session', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
});
